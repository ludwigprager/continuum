# Pinned exactly: reproducibility is a requirement (SPEC 9).
# The rest of the renderer stack (matplotlib, python-pptx, jinja2) arrives
# with M4-M5, not before.
#
# duckdb: `json` and `parquet` are statically linked into the Python wheel and
# need no download. The optional extensions (httpfs, excel, spatial) DO reach
# for the network on first use, so snapshot.py and build_model.py disable
# autoinstall/autoload outright rather than relying on never touching them.
ruamel.yaml==0.19.1
jsonschema==4.26.0
duckdb==1.5.5
pytest==8.4.2

# M3 (Excel). openpyxl is the only way to write xlsx here; LibreOffice is not
# an option (SPEC 11). Pillow is openpyxl's hard dependency for embedding the
# chart PNGs on the dashboard sheet - without it ws.add_image raises.
openpyxl==3.1.5
pillow==11.3.0

# M4 (TXT + PNG). matplotlib renders the charts once, as PNG, and everything
# else embeds them (SPEC 2). Pinned to the patch: matplotlib shifts layout
# between minor versions, which would break byte-identical PNGs (SPEC 9).
# It brings numpy, pillow, fonttools, kiwisolver and pyparsing with it; the
# font cache it builds on first run is baked at image build time (SPEC 8.2).
matplotlib==3.11.2
jinja2==3.1.6
